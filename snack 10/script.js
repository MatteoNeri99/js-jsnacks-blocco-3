// Crea due array che hanno un numero di elementi diversi. 
// Aggiungi elementi all'array che ha meno elementi fino a quando ne avrà tanti quanti l'altro.

const numeriA=[1,2,3,4,5,6,7,8,9,0];

const numeriB=[1,2,3,4];

while(numeriB.length < numeriA.length )[
    numeriB.push(Math.floor(Math.random()*10))
]

console.log (numeriA, numeriB);